(ns knowledge.plates.study-list)

(defn build
  [app-state path state]
  [:div ""])

(def plate
  {:accepts [:study]
   :type :study-list
   :name "study list"
   :group-title "lists"
   :icon "mdi-editor-format-list-bulleted"})
